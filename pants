#!/bin/bash

set -e

if [[ -z "$SKIP_UPKEEP" ]]; then
  ./upkeep
fi

if [ -z "$PANTSBINARY" ]; then
  exec .pantsenv/bin/pants "$@"
else
  export PYTHONPATH=src/python
  exec $PANTSBINARY --print-exception-stacktrace "$@"
fi
