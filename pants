#!/bin/bash

set -e

if [[ -z "$SKIP_UPKEEP" || ! -f .pantsenv/bin/pants ]]; then
  ./upkeep
fi

if [ -z "$PANTSBINARY" ]; then
  exec .pantsenv/bin/pants goal "$@"
else
  export PYTHONPATH=src/python
  exec $PANTSBINARY goal "$@"
fi
